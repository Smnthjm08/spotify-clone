[package]
name = "mini-sealevel"
version = "0.1.0"
edition = "2024"

[dependencies]
rand = "0.10.1"
rayon = "1.12.0"

[dev-dependencies]
criterion = { version = "0.8.2", features = ["html_reports"] }

[[bench]]
name = "scheduler_bench"
harness = false
